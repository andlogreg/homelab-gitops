apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm
spec:
  template:
    spec:
      containers:
      - name: vllm
        resources:
          limits:
            cpu: "2"
          requests:
            cpu: "1"

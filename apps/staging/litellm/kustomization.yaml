apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: litellm
resources:
  - ../../base/litellm
  - ingress.yaml


configMapGenerator:
  - name: litellm-config
    behavior: merge
    files:
      - config.yaml
    literals:
      - LANGFUSE_TRACING_ENVIRONMENT=staging

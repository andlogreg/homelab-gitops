apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gpu-operator
spec:
  interval: 12h
  chart:
    spec:
      chart: gpu-operator
      version: v25.10.1
      sourceRef:
        kind: HelmRepository
        name: gpu-operator
      interval: 12h
      # values:
      #   devicePlugin:
      #     config:
      #       name: time-slicing-config
      #       default: any
